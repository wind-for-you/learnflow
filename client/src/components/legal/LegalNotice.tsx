export default function LegalNotice() {
  return (
    <div
      className="rounded-lg border border-amber-500/40 bg-amber-50 dark:bg-amber-950/40 px-4 py-3 text-sm text-amber-900 dark:text-amber-100 mb-8"
      role="status"
    >
      <strong className="font-semibold">法务提示：</strong>
      以下为占位模板，正式上架前须由法务审核并替换为定稿文本（PR checklist 已标「需法务」）。
    </div>
  );
}
