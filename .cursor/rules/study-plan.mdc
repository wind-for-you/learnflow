---
alwaysApply: true
---
## 通用礼节 (General Etiquette)
- 优先保证代码简洁易懂。
- 别搞过度设计，简单实用就好。
- 写代码时，要注意圈复杂度，函数尽量小，尽量可以复用，尽量不写重复代码。
- 写代码时，注意模块设计，尽量使用设计模式。
- 给我解释代码的时候，说人话，别拽专业术语。最好有图（mermaid风格）
- 帮我实现的时候，需要给出原理，并给出执行步骤，最好有图（mermaid风格）
- 改动或者解释前，最好看看所有代码，不能偷懒。
- 改动前，要做最小化修改，尽量不修改到其他模块的代码
- 改动后，假定10条case 输入，并给出预期结果-给出的mermaid图，必须自检语法，可以被渲染，在暗黑主题上清晰可见-给出的mermaid图，必须要可以被暗黑主题渲染清晰

## 终端执行规则
- 基于powershell的语法
- 多条命令分步执行

# 实验性规则 (Experimental Rule)
当你被要求修复一个 Bug 时，请遵循以下步骤：
1. **理解问题 (Understand):**仔细阅读 Bug 描述和相关代码，复述你对问题的理解。
2. **分析原因 (Analyze):**提出至少两种可能的根本原因。
3. **制定计划 (Plan):**描述你打算如何验证这些原因，并给出修复方案。
4. **请求确认 (Confirm):**在动手修改前，向我确认你的计划。
5. **执行修复 (Execute):**实施修复。
6. **审查 (Review):**查看自己的修改有没有问题。
7. **解释说明 (Explain):**解释你做了哪些修改以及为什么。

Always respond in 中文

## Git 提交与推送约束（必须遵守）
- 推送前必须进行二次安全审计：检查将要推送的 commit 范围（`origin/<branch>..HEAD`）与文件清单。
- 严禁提交或推送任何敏感信息：`.env`、私钥、token、真实 API Key、数据库凭据、云平台密钥。
- 若发现疑似敏感信息，必须先停止推送，告知用户并完成清理（代码移除 + 历史处理建议）后再继续。
- 提交要分批分类：按功能/模块拆分 commit，避免“一个 commit 混入多类改动”。
- commit message 要清晰表达“目的/价值”，优先使用：`feat/fix/refactor/docs/chore/test` 前缀。
- 未经用户明确授权，不允许执行 `git push`。
- 推送后必须回传：分支、commit 列表、远端结果（成功/失败）与下一步建议。

## 项目执行流程规范（需求到测试全流程）
当用户提出“开发/修复/改造”需求时，默认按以下流程执行并在回复中体现阶段产物：

1. **需求分析（Analyze Requirements）**
- 明确目标、范围、非目标、约束条件（时间/兼容性/风险）。
- 若需求不清晰，先提出最小必要澄清问题。

2. **方案设计（Design）**
- 给出可落地技术方案，至少包含：改动模块、接口影响、数据结构影响、兼容策略。
- 对多方案场景，给出取舍依据并推荐默认方案。

3. **开发计划（Plan）**
- 输出分步骤执行计划（可并行/串行、里程碑、验收标准）。
- 明确哪些改动是最小闭环（MVP），优先先达成可运行版本。

4. **实现与自检（Implement & Self-Check）**
- 按最小化修改原则实现，避免无关重构。
- 每完成一段关键改动，进行一次局部自检（类型、lint、关键路径验证）。

5. **测试验证（Test）**
- 至少覆盖：主流程、边界场景、错误分支、回归点。
- 默认提供不少于 10 条测试用例（输入/操作/预期结果）。
- 能运行自动化测试时，优先执行并报告结果；不能执行时说明原因与手工验证步骤。

6. **交付与说明（Deliver）**
- 说明修改了什么、为什么改、影响范围、风险点、回滚方案。
- 提供必要的迁移步骤（如：`prisma db push`、环境变量变更、数据初始化）。

7. **提交与推送（Commit & Push）**
- 按“分批分类”提交，提交前后均执行安全检查。
- 仅在用户确认后推送远端。

8. **复盘与后续（Review & Next Step）**
- 总结当前完成度、遗留问题、下一阶段建议（按优先级）。

## 流程模板化输出格式（默认使用）
当执行需求时，默认按以下模板输出（可按任务复杂度精简，但结构顺序保持一致）：

1. **需求理解**
- 我对需求的理解：
- 目标与范围：
- 非目标（本次不做）：

2. **现状与影响分析**
- 当前实现现状：
- 受影响模块/文件：
- 风险与兼容性影响：

3. **方案设计**
- 方案概述：
- 关键设计点（接口/数据/状态/权限）：
- 备选方案与取舍（如有）：

4. **开发计划**
- Step 1:
- Step 2:
- Step 3:
- 每步验收标准：

5. **实现记录**
- 实际改动文件：
- 核心改动说明：
- 为什么这样改：

6. **测试与验证**
- 自动化测试结果（命令 + 结果）：
- 手工验证结果：
- 10 条关键测试用例（输入/操作/预期）：

7. **交付与运维说明**
- 环境变量/数据迁移要求：
- 发布步骤：
- 回滚方案：

8. **Git 与安全审计**
- 本次 commit 分批清单：
- 推送前安全检查结果：
- 是否已推送及远端结果：

9. **下一步建议**
- P0：
- P1：
- P2：
